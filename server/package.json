{
  "name": "server",
  "version": "0.0.1",
  "description": "socket.io app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "keywords": [
    "example",
    "heroku"
  ],
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^2.1.0"
  }
}
